# Derived artifacts

Query databases and search/graph indexes are generated from canonical logical projection files. Do not hand-edit derived artifacts. The semantic projection identity is the hash of canonical deterministically serialized logical outputs, not arbitrary database file bytes.
