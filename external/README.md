# External adapters

External datasets such as STAPI, rtrek, Memory Alpha, Memory Beta, and transcript indexes enter through adapters/snapshots and crosswalks. External identifiers remain external identifiers. Import success does not automatically promote an external record into canonical corpus truth.

Record source lineage so downstream copies of the same upstream source do not masquerade as independent corroboration.
