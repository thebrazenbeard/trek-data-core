# Trek Legacy State Claims Correction 002

Status: **MIGRATION CORRECTION CANDIDATE — NOT ACCEPTED REPOSITORY STATE**

This tranche quarantines historical project-state and coverage claims recovered from the durable `ChatGPT-Vera-20260817-0934.md` export and related convergence artifacts. It does not migrate Trek story facts.

## Purpose

Legacy conversations reported counters and labels such as `29/158` deep audiovisual ingestion, a `158`-episode inventory, `14` literary Work identities, and broader sibling-worker analysis counts. Those statements are preserved here as historical claims only. Accepted `trek-data-core/main` does not currently contain the governing ledgers or accepted batch state needed to reproduce those counters.

## Rules

- historical worker counters do not become accepted coverage;
- a chat statement that a source was read is provenance about the historical workflow, not proof sufficient for current `CLOSE_READ` admission;
- legacy `STW-*` identifiers remain migration/convergence inputs until accepted Librarian reconciliation binds them;
- `0 SOURCE_BOUND` and `0 BOOK_TEXT` effects remain important negative corrections and must not be erased by larger inventory counts;
- model/general Trek knowledge is explicitly not project research evidence;
- user preferences are quarantined from source selection, salience, coding, hypothesis formation, and evidence weighting;
- sibling-worker agreement has zero independent corroboration weight when it descends from the same source, registry, transformation, or correction stream.

No accepted coverage or identity state is changed by this tranche.
