# Reconciliation

Accepted identity links, scope resolutions, assertion promotions/demotions, and supersessions live here as immutable decisions. New decisions may supersede old ones; accepted historical decisions are not rewritten in place.

The deterministic compiler consumes accepted reconciliation records. LLM calls are forbidden inside the projection build.
