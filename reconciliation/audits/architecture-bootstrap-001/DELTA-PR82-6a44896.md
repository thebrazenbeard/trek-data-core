# Auditor delta — PR #82 head `6a44896`

Date: 2026-08-17
Role: AUDITOR
Accepted `main`: `007641c57933dda222489fff56555f6968ff2a53`
Proposal: PR #82 `architecture/consolidator-v0.1-integration`
Previous audited head: `8491ae38219c23d4517c201a1192963104f15b06`
Current audited head: `6a4489626617e5ddb7ead25493f15143291801db`
Workflow: `validate-core` run `32076279046` — FAILURE

## Delta scope

The current head is two commits ahead of the previously audited head and changes only:

- `tools/test_contract_alignment.py` (+16/-9)
- `tools/test_reconciliation_validation.py` (+24/-12)

No validator, schema, predicate registry, projection compiler, semantic diff implementation, canonical manifest logic, SQLite/PostgreSQL/graph-search builder, or derived-consumer verifier changed in this delta.

Therefore prior implementation findings remain open unless they were findings about the test oracle itself.

## Oracle corrections confirmed

### 1. unittest harness collision — RESOLVED

`ReconTests.run(self, rows)` is replaced by `run_records(self, rows)`, so the test class no longer overrides `unittest.TestCase.run`.

### 2. STABLE -> CONTESTED classification — CORRECTED

The new fixture expects `STATUS_DEMOTED` and explicitly rejects `PROVISIONAL_STATUS_CHANGED` and status-derived `CONFLICT_INTRODUCED`. This matches the current governed root protocol's promotion/demotion vocabulary and avoids treating contested status alone as proof that a conflict set was introduced.

### 3. non-STABLE transition — no fake rank, provisional extension only

The UNRESOLVED -> CONTESTED fixture no longer forces promotion/demotion and no longer derives conflict events from status membership. It expects `PROVISIONAL_STATUS_CHANGED`.

This is acceptable as proposal/test notation only. `PROVISIONAL_STATUS_CHANGED` is not present in the current root governance diff taxonomy, so it must not be treated as an accepted canonical class unless governance explicitly versions/adopts it.

### 4. reconciliation-history-only change — no fact-value laundering

The fixture now rejects `VALUE_CHANGED` and expects `PROVISIONAL_RECONCILIATION_HISTORY_CHANGED`.

Again, the `PROVISIONAL_` prefix correctly signals that this is not yet a root-governed diff class. Acceptance still requires explicit taxonomy/version handling rather than silently freezing this extension through implementation.

### 5. invalid ENTITY_LINK fixture removed

The prior active-decision fixture that attempted to execute an ACCEPTED EXPERIMENTAL `SAME_AS` against an ungoverned `GLOBAL_ENTITY` target is gone. The current active fixture is limited to assertion disposition, assertion projection status, and assertion scope resolution.

### 6. accepted assertion does not require disposition decision to exist

`test_worker_proposed_status_is_not_authoritative` supplies an already-ACCEPTED assertion and no reconciliation decisions, then expects fail-closed `UNRESOLVED` due to missing projection status. This no longer encodes the incorrect requirement that every already-accepted assertion needs a redundant disposition decision before it can be considered.

## Remaining oracle/test-coverage gaps

### A. ENTITY_LINK semantics still lack a valid contract fixture

Removing the invalid `SAME_AS`/GLOBAL_ENTITY case was necessary, but the replacement test set now contains no positive or negative execution fixture proving #61 identity-link semantics:

- ENTITY_LINK subject must be a governed LOCAL_ENTITY;
- relation predicate lifecycle/type must be governed;
- non-merging identity relations must not collapse entities into one identity bucket;
- invalid/experimental/unknown identity relation must fail closed;
- target type/id must resolve under the governed payload contract.

AUD-ARCH-002 cannot close without these behaviors being executable and independently tested.

### B. SCOPE_RESOLUTION coverage remains assertion-only

The current positive scope fixture covers one ASSERTION subject and one `continuity` key. It does not prove:

- governed Work or Local Entity scope subjects where allowed;
- two orthogonal resolution keys for one subject coexist;
- same subject + same resolution key conflicts unless superseded;
- successor same-key supersession leaves history but only one active effect;
- unknown/free-form resolution keys fail closed under the eventual key registry.

### C. predicate governance remains incompletely exercised

The corrected contract tests do not close the previously identified #55 predicate metadata/lifecycle/identity-relation governance surface. Existing validator behavior must still be checked against the accepted contract rather than inferred from these fixtures.

## Implementation findings unchanged

Because implementation files did not change, the previous current-head findings remain:

- AUD-ARCH-001: PARTIAL only. Disposition enum mismatch, scope domains/keys, ENTITY_LINK subject domain, effective-disposition gating, assertion subject domain, path-wide Source/Work ownership, and predicate-governance gaps remain.
- AUD-ARCH-002: OPEN. Compiler remains the pre-#61/#72 implementation and does not execute the corrected typed decision model.
- AUD-ARCH-003: OPEN. Canonical provenance/manifest remains incomplete under #76.
- AUD-ARCH-004: OPEN. Diff implementation remains pre-current contract; corrected red tests now expose rather than conceal that mismatch.
- Derived SQLite/PostgreSQL/graph-search trust findings remain downstream-blocked.

## CI interpretation

Run `32076279046` is correctly RED at this checkpoint. Since the harness collision was repaired and the major known oracle errors above were corrected, this red state is more informative than the predecessor run. It is not architecture failure in itself; it is evidence that the unchanged implementation has not yet caught up with the corrected contract tests.

Do not weaken the corrected red fixtures merely to make CI green. Also do not treat the two `PROVISIONAL_*` diff classes as accepted governance by virtue of being test expectations.

## Exact next frontier

1. Add valid ENTITY_LINK and orthogonal SCOPE_RESOLUTION contract fixtures before claiming the reconciliation oracle complete.
2. Bring validator/schema behavior into #52/#55/#61/#72 alignment.
3. Implement #61/#72 reconciliation application in the compiler.
4. Implement #76 canonical provenance/manifest completeness.
5. Implement governed #67/root diff behavior with explicit handling/versioning of provisional extension classes.
6. Re-run the complete integrated suite through repository validation and all derived deterministic builds, then re-audit the exact green head.

No merge, reconciliation acceptance, accepted-state mutation, or protected effect performed.
