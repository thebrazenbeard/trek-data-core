# Auditor delta — PR #82 validator hardening

Date: 2026-08-17  
Role: AUDITOR  
Accepted `main`: `007641c57933dda222489fff56555f6968ff2a53`  
Integrated proposal: PR #82 @ `d822243bfcf991d56b8089cc1f97ebe1f6627701`  
CI: `validate-core` run `32076498604` — **FAIL**

## Disposition

**CONTESTED / ADMISSION PROGRESS.**

## Confirmed improvements

- governed scope-key registry validation added;
- disposition vocabulary aligned to `PROPOSED|ACCEPTED|REJECTED`;
- predicate metadata/lifecycle validation strengthened;
- typed object-reference shape and duplicate evidence-support validation strengthened;
- authoritative Source/Work records are rejected under worker research partitions;
- the complete 50-test discovery still executes.

## Remaining substantive blockers

1. REJECTED assertion disposition still does not remove an assertion from active projection partitions (#72; AUD-ARCH-002).
2. Typed reconciliation decisions still do not drive projection under #61/#72 (AUD-ARCH-002).
3. Full reachable canonical provenance remains absent (#76; AUD-ARCH-003).
4. Semantic diff still uses obsolete conflict inference/generic value semantics rather than #67 behavior (AUD-ARCH-004).

## Red tests that are stale-oracle defects

- Cross-subject supersession is correctly rejected; the test requires an obsolete exact error substring.
- The supposedly valid batch fixture omits required `subject_type`, so validator rejection is correct and its canonical hash is stale.
- The worker-Source/Work ownership fixture now receives explicit path-level ownership errors as intended, but also retains the stale assertion and asserts an older diagnostic phrase.

## Predicate governance caveat

The registry and validator now require substantially more #55 metadata. Explicit predicate authority scope/level (evidence-description vs research assertion vs reconciliation relation vs multi-level) is still not encoded as a dedicated contract field; `semantic_class` is not a proven substitute.

No merge, implementation mutation, accepted-state mutation, reconciliation acceptance, coverage promotion, deployment, or other protected effect performed.
