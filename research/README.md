# Research partitions

Workers write only inside their assigned partition. Suggested top-level lanes are `tos`, `tas`, `tng`, `ds9`, `voyager`, `enterprise`, `discovery`, `picard`, `lower-decks`, `prodigy`, `strange-new-worlds`, `starfleet-academy`, `films`, and `literature`.

Each bounded batch should contain a manifest and only the local evidence/assertion/entity records produced by that batch. Shared global identities are resolved later.
